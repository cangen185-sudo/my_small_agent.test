# Demo Screenshots

Place final screenshots here before publishing the project to GitHub.

Recommended files:

- `01-readme-reader.png`
- `02-code-checker.png`
- `03-exam-agent.png`

Suggested capture flow:

1. Run `run_app.bat` or `streamlit run app.py`.
2. Open each sidebar tool.
3. Load the built-in sample data.
4. Run the tool and capture the result area.

